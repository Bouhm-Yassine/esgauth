# setup.py
from setuptools import setup, find_packages

setup(
    name='esgauth',
    version='0.1',
    packages=find_packages(),
    install_requires=[
        # List any dependencies if needed
    ],
)
